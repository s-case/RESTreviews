var restreviewsControllers = angular.module('restreviewsControllers', []);

restreviewsControllers.controller('MainCtrl', ['$scope', '$http',
  function ($scope, $http) {

  }
]);

restreviewsControllers.controller('ProductCtrl', ['$scope', '$http',
  function ($scope, $http) {
  }
]);

restreviewsControllers.controller('ReviewCtrl', ['$scope', '$http',
  function ($scope, $http) {

  }
]);

restreviewsControllers.controller('SignUpCtrl', ['$scope', '$http',
  function ($scope, $http) {

  }
]);

restreviewsControllers.controller('UserCtrl', ['$scope', '$http',
  function ($scope, $http) {

  }
]);
